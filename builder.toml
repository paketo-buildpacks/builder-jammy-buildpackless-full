description = "Ubuntu 22.04 Jammy Jellyfish full image with no buildpacks included. To use, specify buildpacks at build time."

[lifecycle]
  version = "0.15.0"

[stack]
  build-image = "docker.io/paketobuildpacks/build-jammy-full:0.0.32"
  id = "io.buildpacks.stacks.jammy"
  run-image = "index.docker.io/paketobuildpacks/run-jammy-full:latest"
  run-image-mirrors = []
